#include <iostream>
#include <type_traits>

#define int decltype(std::remove_cv_t<const volatile decltype(*(((const_cast<signed const *>(reinterpret_cast<int *>(not true xor false or (alignof((float)compl(short)(bool)(nullptr)) and ::operator new(sizeof(char)))))))) bitand (static_cast<long long>([]() -> auto {extern double *_; return delete _, throw "", 0;}())))>{})

// inline static  A_Realy_Long_Named_Variable = 0;

signed main() {
    std::cout << int(0) << std::endl;
    return 0;
}