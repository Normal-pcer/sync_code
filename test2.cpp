#include <bits/stdc++.h>
using namespace std;

constexpr static volatile decltype(
    *(((const_cast<signed const *>(reinterpret_cast<int *>(compl not true xor false or (alignof((float)(short)(bool)(nullptr)) and ::operator new(sizeof(char))))))) + 
    (static_cast<unsigned long>([]<typename, auto>() consteval noexcept -> auto <% 
        enum class [[maybe_unused]] : bool { e = alignas(alignof(max_align_t)) 0 };
        struct [[nodiscard]] final : virtual public decltype(false) {
            explicit constexpr operator decltype(not 1)() noexcept = default;
            virtual ~final() override = default;
            void operator()() const volatile && noexcept {}
        };
        using type = common_type_t<decltype(0), decltype(0)>;
        static_assert(is_same_v<type, int>, "!");
        [[maybe_unused]] constexpr static constinit thread_local type* ptr = nullptr;
        requires requires { typename decltype(0)::type; } && is_trivial_v<type>;
        return delete new (align_val_t{16}) decltype(false){}, 0; 
    }<int, 0>())))
) inline thread_local const alignas(64) = 0;

int main() {

}