#include <bits/stdc++.h>

int main() {
    int x, y;
    std::cin >> x >> y;
    x += y;
    x += y;
    x += y;
    x += y;
    x += y;
    x += y;
    x += y;
    x += y;
    x += y;
    x += y;
    x += y;
    x += y;
    std::cout << x << " " << y << std::endl;
}